<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shinhan.productDetail">
	<select id="productInfo" parameterType="int" resultType="com.shinhan.heehee.dto.response.SellProDTO">
		SELECT PRODUCT_SEQ, ARTICLE_TITLE, PRODUCT_PRICE, INTRODUCE, CONDITION, PRO_STATUS, DEAL, D_CHARGE, SELECT_SEQ, ID
  		FROM SELL_PRODUCT
 		WHERE PRODUCT_SEQ = #{prodSeq}
	</select>
</mapper>