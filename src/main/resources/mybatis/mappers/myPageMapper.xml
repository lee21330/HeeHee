<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shinhan.myPage">
	<select id="saleList" parameterType="string"
		resultType="com.shinhan.heehee.dto.response.SellProDTO">
		SELECT
		sp.ARTICLE_TITLE, sp.PRODUCT_PRICE, sp.PRODUCT_SEQ, img.IMG_NAME
		FROM SELL_PRODUCT sp
		JOIN IMG_FILES img ON img.IMG_CATE_SEQ = 2		
		AND img.TABLE_PK = sp.PRODUCT_SEQ
		AND img.ID = #{USER_ID}
	</select>
</mapper>